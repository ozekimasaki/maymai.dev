---
import logoSvg from '../../assets/images/mayproject/logo_mei.svg';

const year = new Date().getFullYear();

const navItems = [
  { label: 'ABOUT', href: '#about' },
  { label: 'GALLERY', href: '#gallery' },
  { label: 'CHARACTER', href: '#character' },
  { label: 'CONTACT', href: '#contact' },
];
---

<footer class="MpFooter">
  <div class="MpFooter__accent"></div>
  <div class="MpFooter__inner">
    <div class="MpFooter__top">
      <div class="MpFooter__brand">
        <img src={logoSvg.src} alt="桜草メイプロジェクト" class="MpFooter__brandLogo" width="160" height="86" />
        <p class="MpFooter__brandSub">Sakurakusa Mei Project</p>
      </div>
      <nav class="MpFooter__nav" aria-label="フッターナビゲーション">
        <ul class="MpFooter__navList">
          {navItems.map((item) => (
            <li>
              <a href={item.href} class="MpFooter__navLink">{item.label}</a>
            </li>
          ))}
        </ul>
      </nav>
    </div>
    <div class="MpFooter__bottom">
      <p class="MpFooter__copyright">
        &copy; {year} Sakurakusa Mei Project. All rights reserved.
      </p>
      <p class="MpFooter__meta">This is a fictional project.</p>
    </div>
  </div>
</footer>

<style lang="scss">
  @use '../../styles/foundation/mixin' as mix;

  .MpFooter {
    background-color: var(--mp-color-dark);
    position: relative;

    &__accent {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 2px;
      background: linear-gradient(
        90deg,
        transparent,
        rgba(232, 115, 42, .4) 50%,
        transparent
      );
    }

    &__inner {
      display: flex;
      flex-direction: column;
      gap: 48px;
      padding: 64px 64px 48px;

      @include mix.mq(sp) {
        padding: 40px 20px 32px;
        gap: 32px;
      }
    }

    &__top {
      display: flex;
      align-items: center;
      justify-content: space-between;

      @include mix.mq(sp) {
        flex-direction: column;
        gap: 24px;
        text-align: center;
      }
    }

    &__brandLogo {
      height: 60px;
      width: auto;
      display: block;
      opacity: 1;

      @include mix.mq(sp) {
        height: 40px;
        margin-inline: auto;
      }
    }

    &__brandSub {
      margin-top: 8px;
      font-size: .6875rem;
      letter-spacing: .05em;
      color: rgba(255, 255, 255, .55);
    }

    &__navList {
      display: flex;
      gap: 24px;

      @include mix.mq(sp) {
        flex-wrap: wrap;
        justify-content: center;
      }
    }

    &__navLink {
      font-size: .6875rem;
      letter-spacing: .1em;
      color: rgba(255, 255, 255, .6);
      transition: color var(--duration-base) ease;

      @media (any-hover: hover) {
        &:hover {
          color: rgba(255, 255, 255, .9);
        }
      }
    }

    &__bottom {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding-top: 24px;
      border-top: 1px solid rgba(255, 255, 255, .1);

      @include mix.mq(sp) {
        flex-direction: column;
        gap: 8px;
        text-align: center;
      }
    }

    &__copyright {
      font-size: .625rem;
      letter-spacing: .05em;
      color: rgba(255, 255, 255, .5);
    }

    &__meta {
      font-size: .625rem;
      letter-spacing: .1em;
      color: rgba(255, 255, 255, .35);
    }
  }
</style>
