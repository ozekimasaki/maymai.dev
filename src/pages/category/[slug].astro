---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getEmDashCollection, getTaxonomyTerms } from 'emdash';

const { slug } = Astro.params;

if (!slug) {
  return Astro.redirect('/404');
}

const category = (await getTaxonomyTerms('category')).find((term) => term.slug === slug);

if (!category) {
  return Astro.redirect('/404');
}

const { entries: allWorks, cacheHint } = await getEmDashCollection('works', {
  where: { category: category.slug },
});

try {
  Astro.cache.set(cacheHint);
} catch {}

const works = [...allWorks].sort((a, b) => (a.data.order ?? 99) - (b.data.order ?? 99));
---

<BaseLayout title={`${category.label} | Works | Maymai.dev`} description={`${category.label} の制作実績一覧です。`}>
  <section id="category" class="CategoryPage">
    <div class="CategoryPage__inner l-inner">
      <div class="CategoryPage__header u-anime">
        <h1 class="CategoryPage__heading">{category.label}</h1>
        <span class="CategoryPage__label">// category:{category.slug}</span>
      </div>

      <div class="CategoryPage__grid">
        {works.map((work, i) => (
          <a href={`/works/${work.id}/`} class="CategoryPage__card u-anime" style={`--delay: ${i * .1}s;`}>
            <div class="CategoryPage__cardImage">
              <img
                src={work.data.thumbnail}
                alt={work.data.title}
                width="580"
                height="360"
                loading="lazy"
              />
            </div>
            <div class="CategoryPage__cardInfo">
              <h2 class="CategoryPage__cardTitle">{work.data.title}</h2>
              <p class="CategoryPage__cardDescription">{work.data.description}</p>
            </div>
          </a>
        ))}
      </div>
    </div>
  </section>
</BaseLayout>

<style lang="scss">
  @use '../../styles/foundation/mixin' as mix;

  .CategoryPage {
    padding: 128px 0 80px;

    @include mix.mq(sp) {
      padding: 100px 0 60px;
    }

    &__header {
      display: flex;
      align-items: baseline;
      justify-content: space-between;
      margin-bottom: 60px;

      @include mix.mq(sp) {
        margin-bottom: 40px;
      }
    }

    &__heading {
      font-family: var(--font-heading);
      font-weight: 400;
      font-size: 3.2rem;
      line-height: 1.5;
      color: var(--color-text);

      @include mix.mq(sp) {
        font-size: 2rem;
      }
    }

    &__label {
      font-family: var(--font-mono);
      font-size: .625rem;
      line-height: 1.5;
      color: var(--color-label);
      flex-shrink: 0;
    }

    &__grid {
      display: grid;
      grid-template-columns: repeat(2, 1fr);
      gap: 48px 16px;

      @include mix.mq(sp) {
        grid-template-columns: 1fr;
        gap: 40px;
      }
    }

    &__card {
      display: block;

      img {
        width: 100%;
        height: auto;
        aspect-ratio: 580 / 360;
        object-fit: cover;
        transition: transform var(--duration-base) ease;
      }

      @media (any-hover: hover) {
        &:hover img {
          transform: scale(1.03);
        }
      }
    }

    &__cardImage {
      overflow: hidden;
    }

    &__cardInfo {
      margin-top: 16px;
    }

    &__cardTitle {
      font-family: var(--font-heading);
      font-weight: 400;
      font-size: 1.25rem;
      line-height: 1.5;
      color: var(--color-text);
    }

    &__cardDescription {
      margin-top: 8px;
      font-size: .8125rem;
      line-height: 1.8;
      color: var(--color-label);
    }
  }
</style>
